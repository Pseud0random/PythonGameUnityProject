using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ImportantNotes : MonoBehaviour
{
    /*
     * important notes:
     * 
     * Joseph 21/7/22
     * There is a temporary character controller added to Player to allow level 2 to function properly,
     * however when the intended controller has been added this can be removed
     * 
     * I coulden't get the scripts to cooperate for level 1, and loads of things for level 2
     * kept going wrong, so for now i am going to move onto the tutorial level moddeling,
     * and will come back to this when i feel like i wont rage quit software in general =]
     * 
     * */
}
